apiVersion: v1
kind: Service
metadata:
  name: {{ include "express-rest-demo-helm.fullname" . }}
  labels:
    {{- include "express-rest-demo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "express-rest-demo-helm.labels" . | nindent 4 }}